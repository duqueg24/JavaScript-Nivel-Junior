let numeros = [1, 2, 3, 4, 5];
document.write(numeros + "<br>"); //1,2,3,4,5
let resultado = numeros.slice(0,3); //1,2,3
document.write(resultado);// Vuelve una cadena de texto
// primer parametro es desde donde comienza
// sengundo parametro es hasta donde covierte
// sin incluir esa pasicion
